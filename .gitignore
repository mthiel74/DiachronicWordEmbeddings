# ---- Bulk / regenerable data (NOT committed) ----
# HistWords zip + unzipped .npy/.pkl + intermediate matrices and vocab.
# (An inline comment on the same line as a pattern is NOT a comment in
#  .gitignore -- it becomes part of the pattern -- so keep comments above.)
data/raw/
*.zip
*.npy
*.pkl
*.f32
*.bin

# ---- OS / editor cruft ----
.DS_Store
*.swp
*~

# ---- Python ----
__pycache__/
*.pyc
.venv/
venv/

# ---- Wolfram ----
*.mx
# Front-end autosave / build scratch
community/.img-cache/
